# Introduction

This document outlines the design for a system that processes CSV files, with a particular focus on those originating from Japanese sources. The primary purpose of this project is to convert these CSV files into a structured JSON format. This structured data will then be integrated with a Microsoft Dynamics Business Central ERP system.

The core technologies leveraged in this project include Python for data processing and transformation, and Microsoft Dynamics Business Central for the ERP integration.

# System Architecture

This section describes the components and their interactions within the CSV processing and ERP integration system.

## High-Level Diagram

``` mermaid
info


graph TD
    %% Main Data Flow
    CSV[Incoming CSV Files<br>Various Encodings] --> CC[charset_converter.py]
    CC -->|UTF-8 CSV Files| CSVJSON[csv_to_json_converter.py]
    CSVJSON -->|Structured JSON| PJE[process_japan_exports.py]
    PJE -->|Authenticated API Calls| ERP[ERP System<br>Microsoft Dynamics BC]
    
    %% Authentication Flow
    OTH[oauth_token_helper.py] -->|OAuth Token| PJE
    
    %% Currency Conversion Flow
    ERA[exchange_rate_api.py] -->|Exchange Rates| PJE
    
    %% Subprocesses
    subgraph "charset_converter.py"
        CC1[Detect Encoding] --> CC2[Convert to UTF-8]
        CC2 --> CC3[Validate Conversion]
    end
    
    subgraph "csv_to_json_converter.py"
        CSV1[Fix Line Breaks] --> CSV2[Process Headers]
        CSV2 --> CSV3[Process Debit/Credit Pairs]
        CSV3 --> CSV4[Normalize Currency]
        CSV4 --> CSV5[Consolidate Entries]
    end
    
    subgraph "process_japan_exports.py"
        PJE1[Create Journal Lines] --> PJE2[Transform Currency]
        PJE2 --> PJE3[Post to API with Rate Limiting]
        PJE3 --> PJE4[Generate Reports]
    end
    
    subgraph "oauth_token_helper.py"
        OTH1[Acquire Token] --> OTH2[Handle SSL Verification]
        OTH2 --> OTH3[Provide Authorization Header]
    end
    
    subgraph "exchange_rate_api.py"
        ERA1[Get Company Rates] --> ERA2[Find Rate]
        ERA2 --> ERA3[Calculate Cross-Rates]
    end

```

Incoming CSV files --> [charset_converter.py] --UTF-8 CSV--> [csv_to_json_converter.py] --Structured JSON--> [process_japan_exports.py] --Authenticated API Calls--> [ERP System (Microsoft Dynamics BC)]
                                                                                                                                        ^
                                                                                                                                        |
                                                                                                                              [oauth_token_helper.py] --OAuth Token-->


## Component Descriptions

*   **`charset_converter.py`**:
    *   **Responsibility**: Detects the character encoding of input CSV files (e.g., Shift-JIS, EUC-JP). Converts the files from their original encoding to UTF-8 to ensure compatibility and prevent data corruption during subsequent processing steps.

*   **`csv_to_json_converter.py`**:
    *   **Responsibility**: Takes the UTF-8 encoded CSV files as input and converts them into a structured JSON format. This script handles the complex mapping of CSV columns (including a two-line header) to JSON fields, performs data normalization (e.g., currency codes), applies specific business logic for GL account and vendor processing, and truncates descriptions. The output is an intermediate JSON file.

*   **`process_japan_exports.py`**:
    *   **Responsibility**: This is the main ERP integration script. It takes the structured JSON file (generated by `csv_to_json_converter.py`) as input. It then transforms each JSON entry into two journal lines (debit and credit), authenticates with the ERP system (using `oauth_token_helper.py`), maps the JSON data to the ERP API payload structure, and posts the data to Microsoft Dynamics Business Central.

*   **`oauth_token_helper.py`**:
    *   **Responsibility**: Manages the acquisition and caching/refreshing of OAuth 2.0 access tokens required for authenticating API calls to the Microsoft Dynamics Business Central ERP system. This ensures secure communication.

*   **ERP System (Microsoft Dynamics Business Central)**:
    *   **Responsibility**: The target enterprise resource planning system. It receives the processed and structured JSON data, formatted as journal lines, from `process_japan_exports.py`. This data is then used to create general journal entries within the ERP.

## High-Level Data Flow Summary

1.  **Raw CSV Input**: Original CSV files, potentially with various character encodings (e.g., Shift-JIS), are provided to the system.
2.  **Character Set Conversion**: `charset_converter.py` processes these files, detects their encoding, and converts them to UTF-8, saving them as intermediate UTF-8 CSV files.
3.  **Structured JSON Transformation**: The UTF-8 CSV files are then fed into `csv_to_json_converter.py`. This script applies business rules, mappings, and normalizations, transforming the tabular data into a structured intermediate JSON output file.
4.  **ERP Integration**: `process_japan_exports.py` reads the intermediate JSON file. For each record:
    *   It obtains an OAuth token using `oauth_token_helper.py`.
    *   It maps the JSON data to the ERP's API format for journal lines (creating debit and credit lines).
    *   It sends the formatted data to the Microsoft Dynamics Business Central ERP system via its APIs.
5.  **Data Posting in ERP**: The ERP system receives the data and creates general journal entries.

# Detailed Data Flow

This section provides a higher-level overview of the sequential data transformation and integration process, emphasizing the role of each component. The specific business rules and detailed field mappings are covered in the "Business Requirements for Raw Data to API Payload Conversion" section.

**Step 1: Original CSV File Input**
*   The process starts with one or more CSV files. These files are typically exports from other financial systems and may have Japanese-specific character encodings (e.g., Shift-JIS, EUC-JP). They contain transactional data intended for import into the ERP.

**Step 2: Character Set Normalization (via `charset_converter.py`)**
*   **Purpose**: To standardize the character encoding to prevent data loss or corruption ("mojibake") and ensure compatibility with subsequent processing stages.
*   **Process**:
    *   `charset_converter.py` takes an input CSV file path and an output file path.
    *   It uses the `chardet` library to detect the original encoding.
    *   It then reads the file using the detected encoding and writes it out as a new CSV file encoded in UTF-8.
*   **Output**: A UTF-8 encoded version of the original CSV file. This file serves as the input for the next stage.

**Step 3: Conversion to Structured JSON (via `csv_to_json_converter.py`)**
*   **Purpose**: To transform the flat, tabular CSV data into a structured, hierarchical JSON format that accurately represents the financial transactions and incorporates necessary business logic and data cleaning.
*   **Process**:
    *   `csv_to_json_converter.py` takes the UTF-8 encoded CSV file as input.
    *   It processes the specific two-line header structure of the CSV to correctly identify data columns.
    *   It applies various data transformations, normalizations (e.g., currency codes, date formats), and specific business rules for account and department code processing.
    *   It restructures the data from each CSV row into a defined JSON object structure, often creating distinct debit and credit objects within a parent JSON object.
    *   Details of these transformations and mappings are in "Business Requirements for Raw Data to API Payload Conversion".
*   **Output**: An intermediate JSON file containing an array of structured objects, where each object represents the data extracted and transformed from the CSV, ready for ERP integration.

**Step 4: ERP Integration - Posting Journal Lines (via `process_japan_exports.py`)**
*   **Purpose**: To take the structured JSON data, authenticate with Microsoft Dynamics Business Central, and post the data as general journal entries.
*   **Process**:
    *   `process_japan_exports.py` takes the intermediate JSON file (generated in Step 3) as input.
    *   It reads and parses this JSON file.
    *   For each entry in the JSON:
        *   It uses `oauth_token_helper.py` to ensure a valid OAuth 2.0 access token is available for API communication.
        *   It transforms the JSON entry into two separate journal line payloads (one for debit, one for credit) according to the specific field requirements of the ERP API. This includes setting fixed values and mapping fields from the JSON to the API payload.
        *   It dynamically constructs the API endpoint URL based on data within the JSON entry (specifically, the company code derived from the department).
        *   It makes two API calls per original JSON entry to post the debit and credit journal lines to the ERP.
        *   It logs the outcome of each API call.
    *   Specifics of the JSON-to-API payload mapping are in "Business Requirements for Raw Data to API Payload Conversion".
*   **Output**: Data is posted into Microsoft Dynamics Business Central as general journal entries. Log files are updated with the status of these operations.

# Business Requirements for Raw Data to API Payload Conversion

This section details the step-by-step business logic and data transformations applied from the initial raw CSV data to the final JSON payload sent to the ERP system's API. The logic described here is based on the analysis of `csv_to_json_converter.py` and `process_japan_exports.py`.

## 1. CSV Data Ingestion and Initial JSON Structuring (from `csv_to_json_converter.py`)

The `csv_to_json_converter.py` script is responsible for reading the input CSV file, processing its content, and transforming it into a structured JSON format.

### 1.1. CSV Input and Encoding

*   **Expected Encoding**: The script expects the input CSV file to be UTF-8 encoded. Prior processing by `charset_converter.py` is assumed to handle any original encoding issues.

### 1.2. Header Processing

*   **Two-Line Header**: The script is designed to process CSV files with a two-line header.
    *   The first line contains general headers.
    *   The second line contains more specific (often Japanese) headers that are crucial for mapping data to the correct fields. The script reads both header lines to create a combined mapping key for each column (e.g., "ヘッダー1_ヘッダー2").

### 1.3. Data Mapping and Transformation

The script iterates through the CSV rows (starting from the third row, after the headers) and maps them to a structured JSON format. Each CSV row is expected to represent a pair of debit and credit entries or a single-sided entry that will be appropriately structured.

*   **Pairing of Debit/Credit Rows**: The script processes each row to extract both debit and credit side information. For many fields, particularly `department` and `department_code` on the credit side, the values are derived from the debit side's columns in the CSV.
*   **Core CSV Column to JSON Field Mapping (using Japanese Headers from the second header line)**:
    *   `voucher_no`: Mapped from "伝票No." (Voucher No.).
    *   `transaction_date`: Mapped from "仕訳日" (Journal Date), then parsed and reformatted to "YYYY/MM/DD".
    *   `debit.gl_account_type`: Mapped from "借方GL区分" (Debit GL Classification).
    *   `debit.main_account_code`: Mapped from "借方勘定コード" (Debit Account Code).
    *   `debit.sub_account_code`: Mapped from "借方補助コード" (Debit Sub-Account Code).
    *   `debit.account_name`: Mapped from "借方勘定科目名" (Debit Account Name).
    *   `debit.payment_destination_code`: Mapped from "借方支払先コード" (Debit Payment Destination Code).
    *   `debit.department`: Mapped from "借方部門コード" (Debit Department Code).
    *   `debit.description`: Mapped from "借方摘要" (Debit Description).
    *   `debit.amount`: Mapped from "借方金額" (Debit Amount), cleaned of commas.
    *   `credit.gl_account_type`: Mapped from "貸方GL区分" (Credit GL Classification).
    *   `credit.main_account_code`: Mapped from "貸方勘定コード" (Credit Account Code).
    *   `credit.sub_account_code`: Mapped from "貸方補助コード" (Credit Sub-Account Code).
    *   `credit.account_name`: Mapped from "貸方勘定科目名" (Credit Account Name).
    *   `credit.payment_destination_code`: Mapped from "貸方支払先コード" (Credit Payment Destination Code).
    *   `credit.department`: Derived from the debit side's "借方部門コード" (Debit Department Code) column in the input CSV.
    *   `credit.description`: Mapped from "貸方摘要" (Credit Description).
    *   `credit.amount`: Mapped from "貸方金額" (Credit Amount), cleaned of commas.
    *   `applicant_code`: Mapped from "起票者コード" (Applicant Code).
    *   `currency`: Mapped from "通貨" (Currency).

*   **Currency Normalization**:
    *   "台湾ドル" is converted to "NTD".
    *   "円" is converted to "JPY".
    *   "CNY" or "R-CNY" are converted to "R-RMB".

*   **Description Truncation**:
    *   Both debit and credit descriptions are truncated to a default maximum of 100 characters by `csv_to_json_converter.py`. This limit can be configured via the `DESCRIPTION_TRUNCATE_LENGTH` constant in that script. (Note: A further truncation to 50 characters occurs in `process_japan_exports.py` before API posting).

### 1.4. GL Account and Vendor Processing Logic

The script applies specific logic based on the "GL区分" (GL Classification/Type) for both debit and credit sides to determine the `account`, `vendor_code`, and `department_code` fields in the intermediate JSON.

*   **`process_vendor_account` (Applied when GL Type is "Vendor")**:
    *   `account`: Set to the value of `sub_account_code` if present; otherwise, it uses `main_account_code`.
    *   `vendor_code`: Set to the value of `payment_destination_code`.
    *   `department_code` Transformation:
        *   The `department` value (derived from the debit side's "借方部門コード" (Debit Department Code) column in the input CSV for both debit and credit sides) is transformed to create `department_code`.
        *   If the `department` is "VCJ.9999" or "1000", `department_code` is set directly to "VCJ.1000".
        *   Otherwise, `department_code` is formed by taking the first 3 characters of the `department` and appending ".9999" (e.g., "ABCXXX" becomes "ABC.9999").

*   **`process_gl_account` (Applied when GL Type is "G/L Account")**:
    *   `account`: Determined by the first available value in the following order of priority:
        1.  `sub_account_code`
        2.  `main_account_code`
        3.  `payment_destination_code`
    *   `department_code`: The `department` value (derived from the debit side's "借方部門コード" (Debit Department Code) column in the input CSV for both debit and credit sides) is used directly as `department_code`.

### 1.5. Data Validation and Skipping Entries

*   **Department Code Skip Logic**: After the `department_code` is determined (either through `process_vendor_account` or `process_gl_account`), if the resulting `department_code` for *either* the debit or credit side is "VCJ.9999", the entire entry (representing that CSV row) is skipped and not included in the output JSON. This implies that the "VCJ.9999" department code, post-transformation, is considered invalid for further processing.

### 1.6. Credit Department Derivation

*   It is explicitly noted that the `department` and `department_code` fields for the **credit** part of the output JSON are derived from the **debit** side's "借方部門コード" (Debit Department Code) column in the input CSV. This means the credit side's department information in the JSON mirrors the debit side's original department information.

## 2. API Payload Generation (from `process_japan_exports.py`)

The `process_japan_exports.py` script takes the JSON file generated by `csv_to_json_converter.py` and transforms each entry into two journal lines (one debit, one credit) for posting to the Microsoft Dynamics Business Central API.

### 2.1. Journal Line Generation

*   For each entry in the input JSON array, two separate API calls are made to create two journal lines:
    *   One line for the debit transaction.
    *   One line for the credit transaction.

### 2.2. Fixed Values in API Payload

The following fields in the API payload are set to fixed (hardcoded) values for every journal line posted:

*   `Journal_Template_Name`: "GENERAL"
*   `Journal_Batch_Name`: "MIGRATION"
*   `Document_Type`: "" (empty string)
*   `ShortcutDimCode3`: "" (empty string)
*   `ShortcutDimCode5`: "" (empty string)
*   `ShortcutDimCode6`: "" (empty string)
*   `ShortcutDimCode7`: "" (empty string)
*   `ShortcutDimCode8`: "" (empty string)
*   `ShortcutDimCode9`: "" (empty string)
*   `ShortcutDimCode10`: "" (empty string)
*   `ShortcutDimCode11`: "" (empty string)
*   `ShortcutDimCode12`: "" (empty string)
*   `ShortcutDimCode13`: "" (empty string)
*   `ShortcutDimCode14`: "" (empty string)
*   `ShortcutDimCode15`: "" (empty string)

### 2.3. Field Mapping from Intermediate JSON to API Payload Fields

The following table details the mapping from the fields in the intermediate JSON (generated by `csv_to_json_converter.py`) to the fields in the API payload for each journal line:

| API Payload Field           | Derived From JSON Field / Logic (for respective debit/credit part)                                                                                                | Notes                                                                                                                                                                                                                                                              |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `Posting_Date`              | `transaction_date`                                                                                                                                                | Reformatted from "YYYY/MM/DD" to "YYYY-MM-DD".                                                                                                                                                                                                                     |
| `External_Document_No`      | `voucher_no`                                                                                                                                                      | Truncated to a maximum of 20 characters.                                                                                                                                                                                                                           |
| `Account_Type`              | `gl_account_type`                                                                                                                                                 | Direct mapping (e.g., "Vendor", "G/L Account").                                                                                                                                                                                                                    |
| `Account_No`                | If `gl_account_type` is "Vendor", uses `vendor_code`; otherwise, uses `account`.                                                                                 | Truncated to a maximum of 20 characters.                                                                                                                                                                                                                           |
| `Description`               | `description`                                                                                                                                                     | Truncated to a maximum of 50 characters. (Original truncation to 100 in `csv_to_json_converter.py`).                                                                                                                                                              |
| `Currency_Code`             | `currency`                                                                                                                                                        |                                                                                                                                                                                                                                                                    |
| `Amount`                    | `amount`                                                                                                                                                          | For the debit line, the amount is taken as is (expected to be positive). For the credit line, the amount is negated (multiplied by -1).                                                                                                                             |
| `Shortcut_Dimension_1_Code` | First 3 characters of `department`.                                                                                                                               | E.g., if `department` is "ABCDEFG", this becomes "ABC".                                                                                                                                                                                                            |
| `Shortcut_Dimension_2_Code` | If `gl_account_type` is "Vendor", uses `department_code` (which was already transformed in `csv_to_json_converter.py`); else (for "G/L Account"), uses the `department` field from the JSON (which is the original department code from the CSV). | The logic in `process_japan_exports.py` directly uses the `department_code` (for "Vendor") or `department` (for "G/L Account") from the respective debit/credit part of the JSON. The specific transformation for vendors (e.g. to "ABC.9999" or "VCJ.1000") has already occurred in `csv_to_json_converter.py`. |
| `ShortcutDimCode4`          | If `gl_account_type` is "Vendor", uses `vendor_code`; otherwise uses the global `applicant_code` from the JSON entry.                                             | Truncated to a maximum of 20 characters.                                                                                                                                                                                                                           |

### 2.4. Dynamic API URL Construction

*   The script dynamically constructs the API endpoint URL for posting journal lines.
*   It uses the `Shortcut_Dimension_1_Code` (first 3 characters of the `department` field from the JSON, representing the company code) to replace a placeholder in the base API URL (e.g., `companies(XXX)/journals` becomes `companies(ABC)/journals`).

### 2.5. SSL Verification (Technical Detail)

*   The `process_japan_exports.py` script explicitly disables SSL certificate verification (`verify=False`) for both OAuth token acquisition and the subsequent API calls to the ERP system. This is a significant security concern and should be addressed for production environments.

## 3. Note on `currency_modification_report.md` and `modify_currency.py`

The `currency_modification_report.md` document outlines specific currency modification rules, such as converting "VCT" or "NTD" to an empty string for `Currency_Code` when the department is "VCT", and "VCJ" or "JPY" to an empty string when the department is "VCJ". It also refers to a script named `modify_currency.py` intended to implement these changes.

Based on the analysis of `csv_to_json_converter.py` and `process_japan_exports.py`:

*   **These specific currency-to-empty-string modification rules are NOT implemented** in the automated data processing pipeline defined by these two scripts.
    *   `csv_to_json_converter.py` normalizes currency codes (e.g., "台湾ドル" to "NTD", "円" to "JPY") but does not convert any currency code to an empty string based on department codes.
    *   `process_japan_exports.py` directly uses the `currency` field from the JSON (produced by `csv_to_json_converter.py`) as the `Currency_Code` in the API payload, without further modification to an empty string based on department.
*   The script **`modify_currency.py` is not present** in the provided repository structure.

**Implications**:

This discrepancy suggests one or more of the following:
1.  The currency modification rules described in `currency_modification_report.md` (specifically those setting currency to empty based on department) might be outdated, represent a desired future state, or are intended for a manual pre-processing step.
2.  These specific currency modifications might be handled by a separate manual process or an untracked script that is not part of this automated pipeline.
3.  The existing documentation (`currency_modification_report.md`) needs to be updated to reflect the current automated procedures, or the scripts need to be updated to incorporate these rules if they are still required and confirmed as current business logic.

**Conclusion on Currency Modification**: The automated pipeline, as defined by the analyzed scripts, ensures currency codes like "NTD" and "JPY" are present in the API payload (derived from the "通貨" column and normalized) and does not implement the logic to make them empty based on "VCT" or "VCJ" department codes. Any such modification, if occurring, happens outside of the documented automated data flow of these core scripts.

# Design Considerations for Data Format Conversion

A key architectural decision in this project is the conversion of CSV data to an intermediate JSON format before integrating with the Microsoft Dynamics Business Central API. This section discusses the rationale, advantages, and disadvantages of this approach.

**Rationale:**

The primary input is CSV, which, while common, can be ambiguous and lacks a strict structure, especially with variations in headers, column orders, and data types. JSON, on the other hand, offers a well-defined, hierarchical structure that is natively supported by most modern APIs, including the Microsoft Dynamics Business Central API. Converting to JSON allows for a clear, validated, and transformed representation of data before the final integration step.

**Pros of Converting CSV to JSON:**

1.  **Structured Data Representation**:
    *   **Clarity**: JSON allows for a clear, human-readable, and self-describing structure with key-value pairs, making it easier to understand the data compared to relying on CSV column order.
    *   **Complex Data Types**: JSON natively supports complex data types (objects, arrays, booleans, numbers), which can be beneficial for representing structured financial data that might be flattened or awkwardly represented in CSV.
    *   **Schema Validation Potential**: An intermediate JSON format allows for schema validation (e.g., using JSON Schema) before attempting to send data to the ERP. This can catch data issues earlier.

2.  **Decoupling and Modularity**:
    *   **Separation of Concerns**: The conversion process is handled by `csv_to_json_converter.py`, separating the concerns of CSV parsing and initial transformation from the concerns of ERP API interaction (handled by `process_japan_exports.py`).
    *   **Intermediate Data Staging**: The JSON file acts as a staging point. If the ERP integration fails, the already processed and validated JSON data can be reused without reprocessing the original CSV. This is also useful for debugging, as the intermediate JSON can be inspected.
    *   **Future Flexibility**: If the source data format changes (e.g., from CSV to another format) or the target system changes, only the relevant conversion script (`csv_to_json_converter.py` or `process_japan_exports.py`) needs modification, rather than a monolithic script.

3.  **Ease of API Integration**:
    *   **Widely Accepted Format**: JSON is the de facto standard for most web APIs, including Microsoft Dynamics Business Central. HTTP client libraries (like `requests` in Python) can easily serialize Python dictionaries into JSON for API request bodies.
    *   **Reduced Complexity in API Calls**: The `process_japan_exports.py` script can work with clean, structured Python objects (derived from JSON) rather than performing complex CSV parsing and data manipulation simultaneously with API logic.

4.  **Data Validation and Transformation Opportunities**:
    *   The `csv_to_json_converter.py` script can perform significant data validation (e.g., checking for required fields, correct data types, valid values) and transformations (e.g., currency normalization, description truncation, mapping CSV column names to meaningful JSON keys) before the data even reaches the ERP integration stage. This reduces the likelihood of basic data errors causing API failures.

**Cons of Converting CSV to JSON:**

1.  **Intermediate Step Overhead**:
    *   **Performance**: For extremely large CSV files, the process of reading the CSV, converting it to JSON, writing the JSON to disk, and then reading the JSON again can introduce performance overhead compared to a direct CSV-to-API stream (if feasible).
    *   **Disk Space**: Storing intermediate JSON files requires additional disk space, which could be a concern for very large datasets or if many files are processed.

2.  **Increased Complexity (Initial Perception)**:
    *   Introducing an additional script (`csv_to_json_converter.py`) and an intermediate file format might seem to add more "moving parts" to the system. However, this often leads to better modularity and simpler individual components.

3.  **Error Handling in Multiple Stages**:
    *   Errors need to be handled in the CSV-to-JSON conversion stage and then separately in the JSON-to-API integration stage. This requires careful logging and error tracking across the workflow.

**Conclusion for the "toi project":**

For the "toi project," converting CSV data to an intermediate JSON format is a suitable and beneficial approach. The specific complexities associated with the input CSV files (e.g., two-line headers, Japanese character encodings, need for currency normalization and description truncation) necessitate a dedicated transformation step.

JSON provides a robust, structured way to represent this normalized data. This structured intermediate format simplifies the `process_japan_exports.py` script, allowing it to focus solely on API interaction logic using clean, pre-validated data. The benefits of decoupling, improved data validation capabilities, easier debugging, and enhanced maintainability outweigh the potential overhead for the expected scale of this project. The clarity gained from having a well-defined JSON structure before API interaction significantly aids in managing the integration with Microsoft Dynamics Business Central.

# Configuration Management

This section details how system configurations, especially sensitive data, are managed.

**`.env` Files for Local Configuration:**

*   Sensitive information such as API keys, client secrets, and specific URLs for the ERP system are stored in `.env` files.
*   Each developer or deployment environment should have its own `.env` file. This file is placed in the root directory of the project.
*   **Security**: The `.env` file is listed in the `.gitignore` file to prevent it from being committed to the version control system (Git). This is crucial for protecting sensitive credentials from being exposed in the codebase.
*   An example file, `env.example` or `.env.example`, is typically included in the repository. This file lists all the necessary environment variables that the application expects, but with placeholder or empty values. Developers can copy this example file to `.env` and fill in their specific credentials and endpoints.

**`env_config.py` for Loading Configuration:**

*   The `env_config.py` script is responsible for loading the environment variables from the `.env` file into the application's runtime environment.
*   It typically uses a Python library like `python-dotenv` which reads key-value pairs from the `.env` file and makes them available as environment variables (e.g., via `os.environ`).
*   This allows other Python scripts in the project (e.g., `oauth_token_helper.py`, `process_japan_exports.py`) to access configuration values in a clean and secure manner without hardcoding them. For instance, `erp_client_id = os.getenv("ERP_CLIENT_ID")`.

**Key Environment Variables:**

The following environment variables are essential for the system's operation, particularly for interacting with the Microsoft Dynamics Business Central ERP:

*   `ERP_CLIENT_ID`: The client ID for the application registered in Azure Active Directory, used for OAuth 2.0 authentication with the ERP.
*   `ERP_CLIENT_SECRET`: The client secret for the application, used in conjunction with the client ID to obtain access tokens. This is highly sensitive.
*   `ERP_TOKEN_URL`: The URL of the OAuth 2.0 token endpoint for Microsoft Dynamics Business Central (e.g., `https://login.microsoftonline.com/<tenant_id>/oauth2/v2.0/token`).
*   `ERP_API_URL`: The base URL for the Microsoft Dynamics Business Central API (e.g., `https://api.businesscentral.dynamics.com/v2.0/<tenant_id>/<environment_name>/api/v2.0`). This URL is expected to contain a placeholder like `XXX` for the company ID, which is dynamically replaced by `process_japan_exports.py`.
*   `ERP_SCOPE`: The scope(s) required for accessing the ERP API (e.g., `https://api.businesscentral.dynamics.com/.default`). This defines the permissions the application is requesting.

Additional variables might be defined for file paths (e.g., `CSV_FILE_PATH`, `JSON_FILE_PATH` if used by a main orchestrator script, though individual scripts take paths as arguments), logging levels, or other configurable aspects of the system like `DESCRIPTION_TRUNCATE_LENGTH` in `csv_to_json_converter.py`.

# Authentication and Authorization

This section describes the mechanisms used to authenticate with the Microsoft Dynamics Business Central ERP system and authorize API requests.

**OAuth 2.0 Client Credentials Grant Flow:**

*   The system utilizes the OAuth 2.0 client credentials grant flow for server-to-server authentication with the Microsoft Dynamics Business Central API. This flow is appropriate for applications that act on their own behalf, without direct user interaction at the time of API access.
*   **Process (handled by `oauth_token_helper.py` and utilized by `process_japan_exports.py`):**
    1.  The application sends a POST request to the ERP's token endpoint (defined by `ERP_TOKEN_URL`).
    2.  This request includes:
        *   `grant_type`: Set to `client_credentials`.
        *   `client_id`: The application's `ERP_CLIENT_ID`.
        *   `client_secret`: The application's `ERP_CLIENT_SECRET`.
        *   `scope`: The `ERP_SCOPE` defining the requested permissions.
    3.  If the credentials are valid, the token endpoint returns a JSON response containing an `access_token` and its `expires_in` time.

**Role of `oauth_token_helper.py`:**

*   The `oauth_token_helper.py` script encapsulates the token acquisition and management process.
*   **Token Acquisition**: It constructs and sends the token request as described above.
*   **Token Caching**: Upon receiving a new access token, the helper caches it (e.g., in memory or a temporary file) along with its expiry time. This prevents redundant token requests for every API call. Before making an API call, the system checks if a valid, unexpired token is available in the cache.
*   **Token Usage**: When `process_japan_exports.py` needs to make an API call to the `ERP_API_URL`, it requests a valid token. The helper returns the cached token if available and not expired, or acquires a new one if necessary.
*   **Authorization Header**: The obtained access token is then included in the `Authorization` header of API requests to the ERP, formatted as a Bearer token:
    `Authorization: Bearer <access_token>`
*   **Token Expiry**: The logic handles token expiry by checking the `expires_in` time and acquiring a new token when the current one is about to expire or has expired.

**SSL Certificate Verification:**

*   All communication with the ERP system's token endpoint (`ERP_TOKEN_URL`) and API endpoint (`ERP_API_URL`) occurs over HTTPS.
*   **Current Implementation Detail**: As per the analysis of `process_japan_exports.py` (which calls `oauth_token_helper.py`), SSL certificate verification is **explicitly set to `False` (`verify=False`)** for both OAuth token acquisition and subsequent API calls to the ERP system.
*   **Security Implication**: Disabling SSL verification is a significant security risk, especially in production environments, as it exposes the system to man-in-the-middle (MITM) attacks. It means the application does not verify that it is communicating with the genuine ERP server.
*   **Recommendation**: This setting should be enabled (set to `True`) in production and test environments that use valid, trusted SSL certificates. If self-signed certificates are used in development, appropriate measures (like trusting the specific self-signed CA) should be taken instead of globally disabling verification.

**Authorization Scope:**

*   The permissions granted to the application are determined by the `ERP_SCOPE` requested during the token acquisition and the permissions configured for the application's service principal (App Registration) in Azure Active Directory for Microsoft Dynamics Business Central.
*   The principle of least privilege should be applied: the application should only be granted the minimum necessary permissions (scopes) required to perform its tasks (e.g., read/write access to specific general journal APIs).

# Error Handling and Logging

This section outlines the strategies for handling errors and logging activities within the system.

**General Error Handling Strategy:**

*   **Try-Except Blocks**: Core logic in all scripts is enclosed in `try-except` blocks to catch potential exceptions gracefully.
*   **Specific Exceptions**: Scripts aim to catch specific exceptions (e.g., `FileNotFoundError`, `requests.exceptions.RequestException`, `ValueError`, `KeyError`, `json.JSONDecodeError`) rather than generic `Exception` where possible, allowing for more targeted error handling and logging.
*   **Fail Fast/Graceful Degradation**: For critical errors (e.g., inability to load configuration, authentication failure, inability to parse input files), scripts generally "fail fast" by logging the error and exiting. For non-critical errors affecting single records during processing (e.g., an issue with one row in a CSV during JSON conversion, or one JSON entry during API posting), the script may log the error and continue with the next record.

**Error Handling in Specific Scripts:**

*   **`charset_converter.py`**:
    *   Handles `FileNotFoundError` if the input CSV file does not exist.
    *   Handles potential `chardet` errors or low-confidence detection, logging a warning.
    *   Catches `IOError` or `OSError` during file operations.

*   **`csv_to_json_converter.py`**:
    *   Handles `FileNotFoundError` for the input UTF-8 CSV.
    *   Catches `csv.Error` for malformed CSVs or unexpected header structures.
    *   Handles `ValueError` or `KeyError` during data conversion/mapping for individual rows, logging and skipping the problematic row.
    *   Catches `IOError` or `OSError` for issues writing the output JSON file.

*   **`oauth_token_helper.py` (and token logic in `process_japan_exports.py`)**:
    *   Catches `requests.exceptions.RequestException` (including `ConnectionError`, `Timeout`) during token requests.
    *   Checks HTTP status codes from the token endpoint and raises an exception for errors (e.g., 400, 401).

*   **`process_japan_exports.py`**:
    *   Handles `FileNotFoundError` for the input JSON data file.
    *   Catches `json.JSONDecodeError` for invalid JSON input.
    *   Catches `requests.exceptions.RequestException` for network issues during API calls.
    *   Checks API response status codes:
        *   **4xx Client Errors**: Logs the error response (including body if available). Problematic data entries are typically skipped.
        *   **5xx Server Errors**: Logs the error and may implement retries if appropriate (current implementation logs and moves on).

**Logging Mechanisms:**

The system uses Python's built-in `logging` module.

*   **Console Output**: Provides immediate feedback, typically logging INFO level and above.
*   **File-based Logging**:
    *   **`erp_api_integration.log`**: For `process_japan_exports.py` and `oauth_token_helper.py` events (token acquisition, API calls, integration results).
    *   **`csv_conversion.log`**: For `charset_converter.py` and `csv_to_json_converter.py` events (file processing, encoding, CSV parsing, JSON generation).
*   **Log Format**: Includes timestamp, log level, script/logger name, function name (where applicable), and the message. Example: `YYYY-MM-DD HH:MM:SS,ms - LEVEL - SCRIPT_NAME - FUNCTION_NAME - MESSAGE`
*   **Log Levels**: Standard levels (DEBUG, INFO, WARNING, ERROR, CRITICAL) are used.

# Key Scripts and Their Usage

This section provides an overview of the main Python scripts, their command-line usage, and expected inputs/outputs.

**1. `charset_converter.py`**

*   **Purpose**: Detects and converts the character encoding of input CSV files to UTF-8.
*   **Command-line Arguments**:
    *   `input_file_path`: Path to the source CSV file.
    *   `output_file_path`: Path where the UTF-8 encoded CSV file will be saved.
    *   (Optional) `--source_encoding <encoding>`: Manually specify the source encoding.
*   **Example Usage**:
    ```bash
    python charset_converter.py "input/Evelyn Raku export.csv" "output/Evelyn Raku export_utf8.csv"
    ```

**2. `csv_to_json_converter.py`**

*   **Purpose**: Converts a UTF-8 encoded CSV file (with a specific two-line header) into a structured JSON file. It applies business logic for field mapping, data normalization, and transformations as detailed in the "Business Requirements for Raw Data to API Payload Conversion" section.
*   **Command-line Arguments**:
    *   `csv_file_path`: Path to the input UTF-8 encoded CSV file.
    *   `json_file_path`: Path where the output JSON file will be saved.
*   **Input CSV Format Overview**:
    *   Must be UTF-8 encoded.
    *   Assumes a two-line header where the second line's Japanese headers are key for mapping.
*   **Output JSON Format**: An array of JSON objects, structured according to the transformations and mappings defined (see "Business Requirements..." section for details like `debit.*`, `credit.*` substructures).
*   **Example Usage**:
    ```bash
    python csv_to_json_converter.py "output/Evelyn Raku export_utf8.csv" "output/Evelyn Raku export.json"
    ```

**3. `process_japan_exports.py`**

*   **Purpose**: Reads the structured JSON data, authenticates with Microsoft Dynamics Business Central, and posts the data as general journal entries (debit and credit lines).
*   **Command-line Arguments**:
    *   `json_file_path`: Path to the input JSON file (output from `csv_to_json_converter.py`).
*   **Input**: A JSON file as produced by `csv_to_json_converter.py`.
*   **Role in ERP Integration**:
    *   Loads ERP configuration from environment variables.
    *   Acquires OAuth token via `oauth_token_helper.py`.
    *   For each JSON entry, constructs and posts two journal lines (debit/credit) to the ERP.
    *   Detailed field mapping from JSON to API payload is covered in "Business Requirements for Raw Data to API Payload Conversion". This includes specific logic for `Account_No` (using `vendor_code` for "Vendor" type, else `account`) and `Shortcut_Dimension_2_Code` (using transformed `department_code` for "Vendor" type, else original `department`).
*   **Example Usage**:
    ```bash
    python process_japan_exports.py "output/Evelyn Raku export.json"
    ```

**Note**: `oauth_token_helper.py` and `env_config.py` are supporting modules and not intended for direct command-line execution.

# Testing

This section outlines the testing strategy for the system.

**Testing Directories:**

*   **`Tesing/` (Note: should ideally be named `Testing/`)**:
    *   Contains sample CSV files (various encodings, UTF-8 versions), expected intermediate JSON outputs, and potentially sample ERP API responses or validation files.
    *   Used for manual testing, end-to-end workflow verification, and integration testing of script pipelines.
    *   Includes diverse data scenarios: different encodings, two-line header variations, edge cases (empty files, specific data values triggering different logic paths like "VCJ.9999" department codes).

*   **`unittest/`**:
    *   Houses automated unit tests (e.g., using Python's `unittest` framework).
    *   **`unittest/test_process_japan_exports.py`**: Focuses on `process_japan_exports.py`, mocking ERP API calls and `oauth_token_helper.py`. Tests business logic for creating debit/credit pairs, data mapping to API payload (including `Account_No`, `Shortcut_Dimension_2_Code` logic), dynamic URL construction, and error handling.
    *   **Unit tests for `csv_to_json_converter.py`**: Would test header processing, CSV row-to-JSON object conversion, currency normalization, description truncation, `process_vendor_account` and `process_gl_account` logic, and the "VCJ.9999" skip logic.
    *   **Unit tests for `charset_converter.py`**: Would test encoding detection and conversion for various file types.
    *   **Unit tests for `oauth_token_helper.py`**: Would test token acquisition and caching logic, possibly mocking the requests to the token endpoint.

**Testing Workflow and Strategy:**

1.  **Unit Tests**: Run frequently during development. Essential for verifying individual functions and module logic in isolation.
2.  **Integration Testing with Sample Data**: Use data from `Tesing/` to test the scripts in sequence: `charset_converter.py` -> `csv_to_json_converter.py` -> `process_japan_exports.py` (initially against mock ERP endpoints or with validation flags).
3.  **End-to-End Testing**: Test the full workflow with sample data against a dedicated test/staging environment of Microsoft Dynamics Business Central.
4.  **Regression Testing**: Execute all automated tests (unit and potentially automated integration/E2E tests) before releases to prevent regressions.
5.  **Validation of Discrepancies**: Testing should explicitly cover scenarios related to documented discrepancies, such as the `currency_modification_report.md` rules vs. actual script behavior, to ensure the system behaves as currently implemented and documented in *this* design document.

# Conclusion

This design document has detailed a system for processing CSV files, with a special focus on Japanese formats, converting them to a structured JSON, and subsequently integrating this data with Microsoft Dynamics Business Central. The process involves character set conversion, detailed data transformation and normalization as per defined business requirements, and authenticated API communication with the ERP.

The system is modular, with distinct Python scripts handling different stages: `charset_converter.py` for encoding, `csv_to_json_converter.py` for CSV to JSON transformation based on specific business rules, and `process_japan_exports.py` for ERP integration using the generated JSON. These are supported by `oauth_token_helper.py` for authentication and `env_config.py` for configuration management. Error handling, logging, and testing strategies have been outlined to ensure robustness and maintainability.

**Potential Areas for Future Improvement and Maintenance:**

*   **Security Enhancement (SSL Verification)**: Enable SSL certificate verification (`verify=True`) in `oauth_token_helper.py` and `process_japan_exports.py` for all HTTPS communications in production/test environments after ensuring valid certificates are in place.
*   **Documentation Alignment**:
    *   Update or align `currency_modification_report.md` and `README_erp_integration.md` with the actual implemented logic in the scripts, particularly regarding currency modifications and detailed field mappings, or update the scripts if the external documents represent the true desired state.
    *   Correct the directory name from `Tesing/` to `Testing/` in the repository and update references.
*   **Enhanced Test Coverage**: Expand unit and integration tests, especially for `csv_to_json_converter.py`'s complex mapping and transformation logic and for `charset_converter.py`.
*   **CI/CD Integration**: Implement a formal CI/CD pipeline.
*   **Configuration-Driven Mappings**: Externalize CSV-to-JSON and JSON-to-ERP field mappings into configuration files (e.g., JSON or YAML) for easier maintenance.
*   **Batch Processing & Error Reporting**: Improve `process_japan_exports.py` for batching API calls (if supported by ERP) and generating consolidated error reports for failed records.
*   **Idempotency**: Implement idempotency checks in `process_japan_exports.py` to prevent duplicate data posting.
*   **User Interface/Orchestration**: Consider a UI or workflow tool for easier operation and scheduling.
*   **Refine `DESCRIPTION_TRUNCATE_LENGTH`**: Currently, `csv_to_json_converter.py` truncates to 100, and `process_japan_exports.py` further to 50 for the API. Standardize this, likely to 50 earlier in the process if that's the final limit.

Addressing these areas will contribute to a more robust, secure, maintainable, and user-friendly system.
